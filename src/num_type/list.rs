use super::Constant;
use super::Expr;

pub struct ConList {
    list: Vec<Constant>
}

impl ConList {
    pub fn new() -> Self {
        Self {
            list: Vec::new()
        }
    }

    pub fn add()
}